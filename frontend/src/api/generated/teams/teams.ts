/**
 * Generated by orval 🍺
 * Do not edit manually.
 * IncidentHub API
 * REST API for managing service ownership, dependencies, and incident lifecycle
 * OpenAPI spec version: 1.0.0
 */
import type {
  CreateTeamRequest,
  CreateTeamResponse,
  List1Params,
  ListTeamsResponse,
} from '../model'

import { apiFetch } from '../../http'

export const getList1Url = (params?: List1Params) => {
  const normalizedParams = new URLSearchParams()

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  })

  const stringifiedParams = normalizedParams.toString()

  return stringifiedParams.length > 0
    ? `/api/v1/teams?${stringifiedParams}`
    : `/api/v1/teams`
}

/**
 * Returns teams ordered by name and identifier, optionally filtered by activity.
 * @summary List teams
 */
export const list1 = async (
  params?: List1Params,
  options?: Parameters<typeof apiFetch>[1],
): Promise<ListTeamsResponse> => {
  return apiFetch<ListTeamsResponse>(getList1Url(params), {
    ...options,
    method: 'GET',
  })
}

export const getCreate1Url = () => {
  return `/api/v1/teams`
}

/**
 * Creates an active team. The normalized code must be unique.
 * @summary Create a team
 */
export const create1 = async (
  createTeamRequest: CreateTeamRequest,
  options?: Parameters<typeof apiFetch>[1],
): Promise<CreateTeamResponse> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {}
    if (h instanceof Headers) return Object.fromEntries(h.entries())
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      )
    }
    const headers: Record<string, string | readonly string[]> = {}
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) headers[name] = value
    }
    return headers
  }
  return apiFetch<CreateTeamResponse>(getCreate1Url(), {
    ...options,
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(createTeamRequest),
  })
}
